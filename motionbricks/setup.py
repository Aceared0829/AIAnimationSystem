# Modified by the AIAnimationSystem maintainers: added missing and platform-specific dependencies.

raise SystemExit('The install root moved: run pip install -e ".[training,demo]" from the repository root (one directory above).')
