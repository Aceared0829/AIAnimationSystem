# 玩家状态驱动的全身动画：数据、训练、模型与 UE 推理计划

> 状态：P0 已完成，待进入 P1。创建于 2026-09-25。本文件用于持续核对方向和验收条件；执行证据见 [P0 审计记录](experiments/2026-09-25-p0-stance-data-audit.md)。
>
> 项目：AIAnimationSystem。P0 分析基于原工作树 `884f34c`、当时的 `origin/main` `af9f9330`、E 盘审计版数据及训练归档。集成工作区从 `af9f9330` 建立，保留主线已有的数据封版、来源哈希校验和测试；开始后续阶段前仍须核对最新代码与数据身份。本计划本身不表示状态训练、UE 接入或游戏内验收已经完成。

## 一、目标与不可丢失的边界

玩家给出移动意图和 `Stand/Crouch` 请求后，模型连续生成包括**蹲下、蹲伏移动、起身**在内的全身动画姿态。模型接收已显示的姿态历史，并能在过渡中响应方向变化、停步和反向状态请求。长期再扩展 Jump、Traversal、交互动作和更多姿态状态。

实际角色位移、胶囊体尺寸与障碍物碰撞由 UE 的 CMC/Mover 或明确选定的移动系统决定。继续遵守 `pose_only_root_authoritative`：模型输出 Root 相对身体姿态，不能自行把角色移动到与权威 Root 不一致的位置。起身请求被头顶碰撞拒绝时，动画目标必须继续是可执行的蹲伏状态。模型负责生成过渡动作；状态与碰撞系统负责决定允许到达哪里。

最终成功的含义是：**玩家意图能在连续游戏过程中及时改变合理的动画姿态，Root、胶囊体、脚部接触与画面一致，UE 中实测性能达标。**单窗口的离线姿态误差、指定参考帧的零误差或编辑器预览截图均不单独构成完成证据。

## 二、现状：已有证据与缺口

| 部分 | 已有证据 | 当前缺口 |
| --- | --- | --- |
| 数据 | [v1 条件契约](reference-guided-data-contract-v1.md)从 UE 导出保留独立 Root、79 骨骼 Root 相对姿态、30 FPS 时间轴；审计版共 1,722 段、34,733 个 24 帧历史 + 24 帧未来窗口，训练窗口 26,861 个。 | `Crouch` 类别有 394 段，按资产名明确标为 `Stand↔Crouch` 的只有 4 段：训练 3、测试 1，训练侧虽有 24 个窗口，但主要过渡在历史段完成；全部 32 个窗口的未来骨盆高度跨度都小于 10 cm。资产类别和 `Start/Stop` 字样不能直接当作逐帧站蹲状态或玩家按键。 |
| 质量 | [审计报告](../output/conditioned_data_audit_20260924_complete/report.json)排查了形状、时间轴、Root/姿态、接触标签和精确重复；审计版活跃样本的精确动作跨分区重复为 0。用户确认 107 段固定候选全部通过，并要求按逐条标记的结论处理；名单和决定已冻结在 P0 记录中。 | 13 组跨分区 Root 轨道相同；非 Traversal 的语义近似变体及状态过渡样本还需要按来源家族处理。接触标签是启发式结果；UE 场景验收仍需单做。 |
| 条件训练 | 本地 `origin/main` 的 [`ReferenceGuidedPose`](https://github.com/Aceared0829/AIAnimationSystem/blob/af9f9330e6c19ac082d4cbbc92631db573436761/training/pretrain/train_conditioned_pose.py)使用 GRU 历史编码、3 层时间 Transformer、未来 Root 和可选整帧姿态参考，直接预测 24 帧姿态。 | 输入没有玩家期望的 Stand/Crouch 状态。当前训练必须给出原动作未来 Root；模型没有训练契约中预留的期望速度控制模式和逐骨骼参考遮罩。输出位置与旋转分别回归，骨架一致性及脚部稳定性需要单独验收。 |
| 训练对照 | [2026-09-25 对照](https://github.com/Aceared0829/AIAnimationSystem/blob/af9f9330e6c19ac082d4cbbc92631db573436761/training/conditioned_optimization_20260925.md)在同为第 12 轮时，将非固定参考 `(5,18)` 的测试误差从 11.030 降至 7.395 cm；旧采样在无参考和固定双参考场景略好。 | 单 seed、每片段取一个窗口、理想未来 Root 的结果。混合参考不是全面胜出。新模型非固定参考场景的派生接触脚速约 0.986 m/s，源动作约 0.049 m/s，脚部问题仍明显。 |
| UE 链路 | [AIAnimation 实验插件](../unreal-script/AIAnimation/README.md)已有固定采样、ONNX/DirectML 数值验证、动画线程推理、缓冲融合和预览工作台。 | 目前 UE 运行的是**已知源动画的 VQ-VAE 重建**。新条件模型仍是 Python 推理；现有工作台从源动画取得历史及部分辅助骨骼。原 VQ 模型的性能数字不能作为新模型的性能证明。 |
| G1 参考 | [控制器](../inference/demo/controllers.py)将 `C` 变成 `walk_stealth` 模式，[片段表](../inference/demo/clips.py)提供蹲伏目标动作，[生成器](../inference/demo/full_agent.py)根据当前状态与目标约束补全运动。 | G1 的模式主要在模型外选择目标片段和参数；其骨架、权重、MuJoCo 展示与我们 UE 79 骨骼玩家运行时之间仍有数据和工程差距。 |

上述数字是本计划写入时的本机快照。原始审计版契约在 `E:\AIAnimationSystemData\prepared\reference_guided_root_pose_v1_20260924_audited\contract.json`；条件模型归档在 `E:\AIAnimationSystemData\training_runs\conditioned_pose_20260925_{hybrid12,uniform12}`。这些绝对路径仅在本机有效；后续实验须同时保存契约哈希和可复核的小型报告。

## 三、拟定的 v2 在线契约

先覆盖 Stand 与 Crouch，不急于把所有动作塞进一个大枚举。下表为**待实现设计**，形状与频率以 v1 的 30 Hz、24+24 帧为初始候选，须由延迟和动作时长实验确定。

| 字段 | 含义与来源 | 必须注意 |
| --- | --- | --- |
| `history_pose/history_rotations/history_root` | 最近连续显示的身体与权威 Root；启动时可用有效初始姿态填满历史。 | 闭环推理时使用模型已显示的结果，不能不断读取未来源动画。 |
| `requested_stance` | 玩家本次请求：Stand 或 Crouch。 | 输入事件本身不能证明碰撞准许或动画已经到位。 |
| `accepted_stance`、`can_uncrouch` | CMC/Mover 对胶囊体与头顶空间的实际结果。 | 起身受阻时不可向模型发送“必定站起”的目标。 |
| `locomotion_intent` | 期望局部移动方向、速度、朝向、步态及可选风格。 | 状态与步态分开表示；蹲伏行走和原地蹲伏都能表达。 |
| `future_root_plan` | 控制器预测的短时 Root 路径，或先选一个真正可在运行时提供的速度控制模式。 | 原动作未来 Root 只用于理想上界；线上不得取得真值。预测路径与实际执行路径要分别记录。 |
| `timed_reference`（可选） | 可执行的目标姿态、落脚或交互关键帧及到达时间。 | 普通站蹲切换必须有无参考能力；测试参考不能从待预测的真值偷取。 |
| 输出姿态 | Root 相对的未来全身姿态；可附脚接触概率、过渡进度和有效性信息。 | 需要恢复 UE 层级局部变换，并补齐当前预览中依赖源动画的辅助骨骼。权威 Root 始终由控制器提供。 |

`requested_stance`、物理上已获准的 `accepted_stance` 和从历史姿态观察到的视觉过渡进度是不同变量。状态机只管理请求、合法转换、碰撞和网络同步；它不代替模型播放一段固定的蹲下/起身动画。可选目标姿态库可像 G1 演示那样提供目标，模型仍须生成从当前动作到目标的合理中间帧。

## 四、实施阶段与退出条件

### P0：冻结基线并核实数据

- [x] 记录原审计分支、提交 SHA、脏工作树、E 盘数据契约 SHA、训练权重 SHA、评估脚本版本和硬件。集成工作区从 `origin/main` 派生，只移植 P0 证据与视口改动，不覆盖主线封版及旧工作区。身份和哈希见 [P0 审计](experiments/2026-09-25-p0-stance-data-audit.md)。
- [x] 用现有 Motion Review 入口核对 107 段固定候选：SQLite 仍为 107/107 `approved`；用户在 2026-09-25 确认全部通过，并要求按逐条标记的结论处理。P0 以[按 clip ID 冻结的确认清单](experiments/assets/2026-09-25-p0-stance-data-audit/restart_2026-09-25/review_acceptance.csv)关闭该项；58 条旧批量备注保留作历史，没有伪造逐条点击或播放记录，也未改写 SQLite。UE 场景的蒙皮、碰撞和落点验收另在后续阶段完成。
- [x] 对 4 条明确 `Stand↔Crouch` 动作逐帧看侧视姿态与 Root，确认资产名、动作方向、主要过渡帧及原地边界。4×76 帧图和逐窗口数据见 [P0 审计](experiments/2026-09-25-p0-stance-data-audit.md)；UE 蒙皮/碰撞验收另做。
- [x] 复核跨分区近似变体和 13 组 Root 同轨，形成[来源家族草表](experiments/assets/2026-09-25-p0-stance-data-audit/source_family_draft.csv)；保留原 v1 测试集与基线报告。语义家族的最终归并与 v2 重划分留到 P1，不能将同 Root 直接等同同来源。

**退出条件：**关键样本有人工结论；知道哪些过渡数据是真实可用的；拥有不会与历史结果混淆的数据/代码/权重身份记录。

### P1：建立真正的状态过渡数据集

- [ ] 新建独立 v2 契约，不改写 v1 原始 NPZ、统计量、旧检查点或历史报告。
- [ ] 标注每帧观察到的 `Stand/Crouch/Transition`、过渡方向与起止点；记下步态、移动方向、速度、脚相位和接触。人工验证关键边界。`Start/Stop` 资产名须先确认语义。
- [ ] 区分观察到的动作与真实输入。旧动画只能给出**推定请求时刻**；在 UE 中另采玩家输入、CMC 批准/拒绝、实际 Root、连续生成姿态与碰撞结果。
- [ ] 优先补独立来源的原地站蹲、移动中切换、起步/停步切换、反向打断和受阻起身。用覆盖矩阵登记风格、速度、方向、左右脚相位，按缺口补采。窗口重采样和镜像可扩充训练视角，但不能充当独立来源；时间变速须同步变换 Root、姿态与接触。
- [ ] 按原始录制序列或语义家族划分 train/validation/test，并另留站蹲过渡专用测试场景。冻结后只用验证集选方案。

**退出条件：**每个核心过渡场景在留出集中有独立动作来源；逐帧状态、命令来源与分区可审计；不足的场景明确标记“尚不能声称泛化”。

### P2：先改控制输入，再做模型消融

固定 v2 数据与评估窗口，单次实验尽量只改变一项；保留现有 256 宽度模型作为对照。

| 编号 | 实验 | 要回答的问题 |
| --- | --- | --- |
| B0 | v1 条件模型直接评估 v2 站蹲场景 | 当前权重具体在哪些过渡、速度和脚相位失败？ |
| B1 | 增加 `requested/accepted stance` 与步态条件；其余结构尽量保持 | 显式状态是否让无参考的蹲下、起身稳定出现？ |
| B2 | 按过渡方向/阶段采样，保留普通循环样本 | 稀有事件能否提高，同时不损害 Walk/Run/Idle/Crouch 稳态？ |
| B3 | 加历史到未来首帧的接缝损失，再分别测 FK/骨长与接触约束 | 连续性、脚滑与骨架合理性分别由哪项改善？ |
| B4 | 增加可在线取得的目标姿态或短时 Root 计划扰动/实际控制轨迹 | 从理想离线条件迁移到真实控制后还剩多少质量？ |

- [ ] 主要评估同时包含**无参考**和**可执行参考**；有参考帧被硬覆盖时，单独报告非参考帧效果、接缝与脚滑。硬覆盖后的锚点零误差不是模型预测成绩。
- [ ] 在验证集选择候选，关键结论至少以多个随机种子复核；冻结测试集只用于最终确认。分别保存理想 Root 与真实控制 Root 的结果。
- [ ] 当前整帧硬参考与契约预留的逐骨骼遮罩不要混称。需要局部骨骼约束时，重新训练对应遮罩条件。
- [ ] 若位置/旋转独立回归造成骨长或层级不一致，再比较旋转驱动并经 FK 得到位置、或潜变量解码；没有证据前不先扩大参数量。

**退出条件：**至少有一个方案在专用站蹲测试中完成双向过渡，稳态动作没有不可接受退化；关键收益能从等条件消融解释，而非只来自更多轮数或测试集调参。

### P3：连续推理与游戏控制闭环

- [ ] 建立固定脚本：Stand→Crouch→蹲走→停步→Stand、过渡中反向输入、快速转向、不同脚相位、低帧率、起身受阻、Root 预测误差与网络修正。
- [ ] 每次重规划都使用已经显示的姿态历史和最新获准的控制状态；测试多次窗口接续的误差、抖动、状态响应和长时间漂移。
- [ ] 评估 2/4 帧重规划、重叠融合和不同播放缓冲的质量/响应权衡。状态改变时可立即安排一次重规划；记录从按键到首个可见姿态变化的延迟。既有实验中的 8 帧约 267 ms 延迟不能直接沿用。
- [ ] 为无效数值、推理超时、模型/骨架不匹配、网络回滚建立可观察的安全回退，并保证恢复时没有明显姿态突跳。

**退出条件：**连续序列的状态响应、脚部、接缝和 Root 对齐通过人工与数值复核；所有失败和回退有计数与可复现日志。

### P4：新的条件模型进入 UE

- [ ] 为新模型导出固定形状 ONNX 与 manifest，绑定骨架顺序、单位、坐标、统计量、状态编码、窗口长度、权重 SHA 和输出版本；比较 PyTorch、ONNX Runtime CPU、目标 GPU 后端的相同输入输出。
- [ ] 先在目标机器量测新模型算子支持、CPU/GPU 分配、P50/P95 延迟、上传下载与多角色负载。DirectML 对固定输入形状更有利，同一 session 的运行调用须符合其并发约束；以后端实测决定调度方式。
- [ ] 以玩家状态、CMC/Mover Root、已生成历史作为 UE 推理输入；把预测姿态恢复为 UE 骨架层级并处理辅助骨骼、蒙皮、脚 IK、碰撞及错误回退。复用当前插件的数值夹具、时间轴与预览工具，但不把源动画当作玩家驱动输入。
- [ ] 在真实角色/关卡中分别验证空旷地面、低矮障碍、斜坡、不同帧率和多角色；若计划多人使用，再验证 CMC/Mover 权威、预测与状态同步。

**退出条件：**新条件权重在 UE 玩家控制中实际运行，双向站蹲和打断场景通过；数值对齐、渲染观感、碰撞、推理预算和连续运行报告齐全。

## 五、每轮必须看的验收表

| 维度 | 必记指标或结果 | 常见误判 |
| --- | --- | --- |
| 状态 | 请求、批准、首帧响应、达到目标状态、被拒绝及反向打断次数 | 把按键状态直接当作角色已经站起或蹲下。 |
| 姿态 | 非参考帧关节误差、旋转误差、骨长/FK、历史与预测接缝 | 只看被硬覆盖的参考帧零误差。 |
| 时间 | 姿态速度/角速度、局部尖峰、连续多窗口运行 | 只评估单个中心窗口或只追求更低的二阶差分。 |
| 接触 | 左右脚接触准确性、接触帧世界脚速、穿地及 IK 修正量 | 将启发式接触标签当作物理真值；用位置误差代替脚滑评价。 |
| Root 与场景 | 预测 Root 与 CMC 实际 Root 偏差、胶囊体碰撞、头顶净空 | 使用原动画未来 Root 的离线分数代表可玩的运行时效果。 |
| 性能 | 按键到可见变化、推理 P50/P95、整帧耗时、多角色负载 | 将旧 VQ-VAE 的约 2 ms 测量移植到新条件模型。 |
| 泛化 | 独立动作家族、不同风格/速度/方向的逐场景结果 | 用重复窗口数量冒充独立动作数量。 |

每次实验写清数据/权重/代码哈希、命令、设备、随机种子、配置、对照和失败。**计划保持在本文件；真正执行的实验另按 [`record-research-experiments`](../.agents/skills/record-research-experiments/SKILL.md) 记入 `docs/experiments/`。**未经 UE 玩家闭环验证，不宣称已经解决真实游戏中的蹲下起身。

## 六、遇到问题时的决策规则

1. **站蹲样本不够或标签不可靠：**先补独立数据与人工复核；不要靠无限重复现有 3 条训练过渡来“解决”。
2. **理想 Root 好、真实控制 Root 差：**先查轨迹预测、控制契约及训练/推理条件一致性，再判断是否需要改模型。
3. **离线误差好、UE 画面差：**按 Root↔Pelvis、坐标与参考轴、层级骨骼、蒙皮、脚接触、碰撞的顺序定位；保留实际截图与日志。
4. **硬参考到位、过渡不顺：**检查历史接缝、参考时刻分布、连续重规划及脚相位；不要只增加锚点覆盖强度。
5. **新模型超出帧预算：**先看执行后端和 CPU/GPU 拆分、固定形状、调用频率和缓冲，再考虑蒸馏或轻量结构；任何降频都必须重新测输入响应。
6. **试验收益互有取舍：**报告各场景收益与代价，保留无参考、固定参考、非固定参考以及站蹲过渡的分项结果；不以一个平均分替代选择。

## 七、现在从哪里开始

P0 已重新核对并完成。下一项实际工作是 **P1 的 v2 逐帧状态字段与补采覆盖矩阵**：先修复站蹲主过渡没有进入未来预测窗口的问题，确认哪些运行时状态信号能稳定取得，再决定独立来源动作的补采量和第一轮 B0/B1 对照的样本与验收标准。
